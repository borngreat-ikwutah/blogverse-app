meta {
  name: Unfollow User
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/api/user/{{targetUserId}}/follow
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  targetUserId: replace-with-user-id
}
