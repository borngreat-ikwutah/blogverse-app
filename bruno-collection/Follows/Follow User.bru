meta {
  name: Follow User
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/user/{{targetUserId}}/follow
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  targetUserId: replace-with-user-id
}
